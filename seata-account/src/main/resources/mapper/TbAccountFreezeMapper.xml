<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dolphin.account.mapper.TbAccountFreezeMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.dolphin.account.entity.TbAccountFreeze">
        <id column="xid" property="xid"/>
        <result column="user_id" property="userId"/>
        <result column="freeze_money" property="freezeMoney"/>
        <result column="state" property="state"/>
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        xid, user_id, freeze_money, state
    </sql>

</mapper>